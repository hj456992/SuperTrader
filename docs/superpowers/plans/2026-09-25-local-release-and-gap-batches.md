# 已验收版本本地发布与后续批次计划

> 执行方式：沿用已有产品、前端、后端、测试、运维五个任务，由监督任务统一集成与验收。按 executing-plans 分步执行；本计划不授权一次性实现全部后续可选能力。

**目标：** 将已验收并上传的画像 Agent 版本部署到实际使用入口，并把后续缺口拆成有边界、可验证的批次。

**架构：** 保持单个本地 Java 插件宿主、既有 PostgreSQL、现有前端和 DSH／AgentScope。先发布固定候选，不混入尚未验收的新功能。后续按业务价值、兼容性和工作量逐批实施。

**技术栈：** Java 17、DSH／AgentScope、PostgreSQL、JavaScript／Cordis、Python、Maven、Node。

## 授权与约束

- 用户要求团队继续推进，架构任务负责讨论，监督任务负责分工、集成和本地落地。
- 当前发布候选为 `4754cb10e45478a1347c1b1a49e5078829f86f59`；此前已上传 GitHub，不重复上传。
- 保留全部工作树；不覆盖未提交改动，不清空业务数据，不使用真实聊天作测试夹具。
- 运维独占原目录同步、制品切换和进程操作；TL 核对准备与 QA 结论后发放执行指令。
- 备份和运行记录保存在 Git 忽略的私有目录，目录 0700、敏感文件 0600；凭据及真实内容不得进入报告。
- 不新增服务、数据库、付费依赖、公共底座改造、多 Agent、平台全历史订阅。超出既有栈的重大工作先给用户说明影响和最小替代方案。

## 第一批：本地使用入口发布

### 1. 核对版本与原入口

- [x] TL 读取远端：`origin/main` 为 `4754cb1`。
- [x] TL 核对集成树：`ailiao/tl-agent-upgrade` 为 `4754cb1`，干净。
- [x] TL 核对原目录：`outputs/demo` 的 `main` 为 `b410f0f`，干净。
- [x] TL 核对原端口：48740 由 PID 99465 监听；进程加载制品的具体版本由运维进一步核验。
- [ ] 运维核对原启动入口、父进程、底座制品、运行配置及活跃任务，只报告脱敏元数据。

### 2. 保护与发布门槛

- [ ] 运维保留旧版本源码／JAR／前端／启动制品及校验和，给出精确回退命令。
- [ ] 运维完成必要业务数据私有备份，验证备份可读及权限；不在业务库演练恢复。
- [ ] QA 核对候选相对已验收生产源差异及旧数据兼容，完成独立确定性发布检查。
- [ ] 前端明确只读页面检查范围，不生成画像、不采集平台、不修改真实数据。
- [ ] TL 检查无发布阻断，确认没有进行中的业务任务，向运维发放切换指令。

### 3. 切换与验收

- [ ] 运维在原目录保持干净且可快进的前提下同步固定候选；有变化则停止覆盖并报告。
- [ ] 运维仅停止已核对归属的原实例，保持原端口和原业务数据配置，使用兼容制品启动候选。
- [ ] 运维确认插件 active、HTTP 静态入口、业务元数据及数据计数符合切换前记录。
- [ ] 前端检查入口与资源加载，只读确认新界面能力，避免展示真实材料。
- [ ] TL 独立核对目录提交、运行制品与监听进程；验收失败按具体回退方案处理，不自动恢复数据库覆盖新数据。

## 第二批：缺口拆解与最小实现

- [ ] 产品完成价值、最小实现、依赖、粗估工作量、验收和范围状态清单。
- [ ] 后端提供攻略执行一致性、检索／分段、来源身份、依赖冲突、持久恢复的代码依据及最小方案。
- [ ] 前端提供状态展示和交互的最小改动及验收范围。
- [ ] TL 对齐首个可收敛批次的具体设计和实施计划，按现有授权推进；显著扩大范围的方案交用户决定。
- [ ] 后续每批先固定验收条件，再实施、独立测试、集成；文档不能代替功能交付。

## 文档与状态

- [ ] 更新 README 与发布记录，分别列明“已实现／已验证／已发布／待实施／待用户决定”。
- [ ] 保留旧验收记录的历史语义，新建本地发布记录，不把旧“未发布”叙述改成当时已发布。
- [ ] 最终报告明确实际运行版本、数据保护、验证范围、剩余缺口和是否存在用户决策项。

记录文件：`docs/ops/local-release-20260925.md`（运维）、`docs/qa/local-release-review-20260925.md`（QA）、`docs/frontend/local-release-checklist-20260925.md`（前端）、`docs/product/architecture-gap-backlog-20260925.md`（产品）、`docs/backend/architecture-gap-assessment-20260925.md`（后端）。文件由各自任务完成后，TL 审阅集成。
