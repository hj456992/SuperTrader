# 产品符合性独立审阅：集成及前端复验

日期：2026-09-24。审阅者：产品角色。协调与汇报对象：执行 TL `01a0d244-0bdd-71e2-b824-6ac31bb31078`。

## 审阅对象与结论边界

- 起始集成 `790a6b1`；执行 Node 测试及交互探针时集成已为 `9be8679ed47be50c760cd6719d7b8dc99fe3cee1`，运行前后 SHA 一致。两个提交之间 `web/src/` 与 `tests/acceptance/` 无差异。
- 基线为 [A01–A23 产品验收](profile-agent-acceptance.md)；审阅集成中的前端交付、QA 前端复核、QA 报告，以及后端 `1c345b2`、运维 `589728d` 的交付文档。对关闭提示问题另追踪了后端真实 `cancel` 实现。
- 结论：前端范围保持收敛；首轮发现的关闭提示缺陷与取消文案错误，已在集成 `7cf1ab5` 独立复验关闭，最新 Node 56/56 通过。**不签署全部 A01–A23 通过或发布通过。** 真实模型、数据库保存/重启、真实浏览器重开及完整服务链仍须独立证据。
- 本轮仅新增本审阅文档，不修改实现、公共接口、底座或其他角色文件，不新增角色。

## 实际执行的证据

命令在产品工作树执行，引用集成文件的绝对路径：

```sh
node --test /Users/hou/Documents/Codex/2026-09-22/garden-product-design/work/ailiao-upgrade/tl/web/src/*.test.js /Users/hou/Documents/Codex/2026-09-22/garden-product-design/work/ailiao-upgrade/tl/tests/acceptance/*.test.mjs
```

结果：**53 tests / 53 pass / 0 fail / 0 skipped**，退出 0。覆盖旧画像、主体隔离、书籍与事实分区、知识状态、转义、阶段事件、前端取消请求/切换竞态以及卸载行为。DOM、HTTP、计时器由测试替换；不是服务端或真实浏览器端到端验证。

另用 Node 内存探针导入真实 `ranch-ui.js` 的 `apply` 和 `ranch-view.js` 的 `renderRanch`：

1. 为 `error` 和 `interrupted` 各创建虚构任务 `r`，HTTP 状态响应始终返回该终态（与当前后端 cancel 行为一致）。
2. 挂载真实前端，点击界面 `data-action=cancel`、`data-run-id=r` 的「关闭提示」。
3. 确认发送一次取消请求，刷新后「关闭提示」仍在；两个终态均复现。
4. 另渲染 `cancelled`、人物不存在任何 `profile` 的状态，确认仍输出「已保存的画像仍可查看」。

探针输出三项均复现，退出 0（断言确认缺陷存在，**不是修复通过**）。探针只替换运行边界，无真实网络、私有材料或磁盘夹具。未运行本轮 Java/运维测试、构建或真实模型；其他角色文档中的结果只视为待汇总证据，不计入本人的执行结果。

## 首轮问题记录（已由下文复验关闭）

### PRD-RV-01 · P2 · 错误/中断提示不能关闭

- 对应：A17/A20/A22，既有错误提示关闭能力及新中断状态。
- 集成位置：`web/src/ranch-view.js:46` 为 `error/interrupted` 渲染「关闭提示」且仍使用 `cancel` action；`web/src/ranch-ui.js:114–120` 对该操作调用取消 API 后重新读取状态；`src/main/java/dev/garden/RanchAnalyzer.java:103` 对非 `running/cancelling` 直接返回。
- 触发：画像失败或重启后显示中断，用户点击「关闭提示」。
- 实际：取消接口不改变终态，刷新后提示和按钮原样显示。人物已删除时也无法通过现有按钮关闭遗留错误。
- 最小修复方向：由 TL 协调现有前端实现按任务 ID 关闭当前提示（或沿用一致的既有终态关闭约定）；不要把完成状态改成取消成功，不放松定向取消、保存屏障或新任务隔离，不需要新接口。
- 复验：error/interrupted 的按钮确实关闭当前提示；后续新 run 的提示仍出现，旧关闭动作不得吞掉新 run 的状态。

### PRD-RV-02 · P3 · 首次取消暗示存在旧画像

- 对应：A14/A15 及最小取消文案。
- 集成位置：`web/src/ranch-view.js:43` 的 `cancelled` 文案无条件为「任务已停止，已保存的画像仍可查看」。
- 触发：人物没有任何已保存画像，首次生成被取消。
- 实际：页面同时显示空画像和上述已保存提示；文案未明确「本次结果未保存」。
- 最小修复：采用基线「任务已取消，未保存本次结果」；如保留旧画像说明，仅在确有旧画像时追加。不得因文案修复新增恢复、历史或自动重试功能。
- 复验：无旧画像与有旧画像各一例；完成先于取消的任务仍按 done 显示，不能误报未保存。

## 范围及文案核对

| 项目 | 审阅结论 |
| --- | --- |
| 本人/对象入口、目标表单 | 保留原入口、材料归属和选择；没有另建身份或关系档案产品 |
| 目标独立提示 | 产品基线要求的「交往目标用于相处建议，不改变人物事实」尚未出现在现有目标表单；属于原范围内文案遗漏，由 TL 安排现有前端补齐，不要求新增交互 |
| 书架与上传成功提示 | 已同时提到画像和攻略；原 R01 文案风险有源代码和测试支持，可关闭该文案项 |
| 策略说明 | 已改为目标画像、双方材料、关系目标与启用资料；没有要求策略接入本人画像 |
| 书籍与事实 | 分区展示、明确“书籍提供理解方法，不是这个人的事实证据”；无书/未命中可区分，旧画像不伪造知识状态 |
| 推断、覆盖、反证、未知 | 前端具备 kind、coverage、scope、confidenceReason、counterEvidenceIds 和 uncertainties 展示；真实内容是否正确由后端及模型链路验收 |
| 删除确认 | `7cf1ab5` 已明确引用该书的当前及历史整份画像会被移除，与后端交付的删除行为一致；文案与交互测试不能代替真实删除持久化验收 |
| 进度 | 保留轮询和实际阶段字段；没有新增百分比、Token 流、SSE、通知或自动续跑承诺 |
| 状态文案 | 同义且准确的简短阶段文案不按逐字差异判失败；关闭提示和取消事实错误已按下文复验关闭 |
| 预算失败原因 | 已在 `838c21f` 源码复核关闭：第 7 步前设置预算标记并抛出专用 `ProfileRuntime.BudgetExceeded`，收尾后仍保留该原因；RanchAnalyzer 映射为「已达到6步预算，本次结果未保存。」。不再按旧实现保留文案缺陷；证据边界见末节 |
| 基础设施 | 交付说明复用既有进程内插件与文档表；本轮未提出新增服务、数据库、付费依赖、公共底座改造或业务扩项。真实装配可行性不由文档推断 |

## A01–A23 逐项证据账

“部分”仅指列出的环节有本次独立证据，不能记为整项通过。后端交付后的旧 QA 失败结论需要在新提交重跑，不能机械沿用旧失败，也不能因已声称修复就改成通过。

| ID | 本轮结论 | 已有独立证据 | 仍需提交的验收证据 |
| --- | --- | --- | --- |
| A01 | 部分 | 当前/历史本人引用跨人物 me，排除 them/context 的 Node 断言通过 | 本人实际工具循环、生成、校验和正式保存；知识状态及 UI 读取一致 |
| A02 | 部分 | 对象及历史/反证引用隔离、切换不串书摘通过 | 对象端到端生成保存，选择目标后事实仍独立 |
| A03 | 部分 | 方法/事实分区、书籍 ID 不解析成人物事实通过 | 保存入口对错误类别引用拒绝；真实结果不以书籍观点定义人物 |
| A04 | 待后端复验 | UI 策略范围文案已正确；本次未执行 Java | 在新入口重跑仅改目标时输入/现存事实不变，并验证既有建议可使用目标 |
| A05 | 未独立验收 | 可显示 uncertainties/coverage 不等同资料不足真实流程 | 零适格材料、少量材料、仅背景/转述时明确不足，不捏造完整画像 |
| A06 | 部分 | 反证、范围与不确定性渲染通过 | 工具观察确实修正冲突判断，完整输出保留限制且能保存 |
| A07 | 部分 | empty_library 显示区别于 no_match | 空库及全停用时最终状态、有限画像及无伪造书摘 |
| A08 | 部分 | no_match 文案区别于空库 | 启用书籍但无命中的真实生成链与状态 |
| A09 | 未独立执行 | 只读接入文档及 ProfileRuntime，看到实际 AgentRegistry 路径 | 两条路径真实插件配合可控模型的循环证据；至少一例观察改变后续输出；实际启动装配及真实模型结果分列 |
| A10 | 部分 | 前端本人/对象来源隔离、用户备注标注通过 | 最终模型输出和保存校验维持转述 inferred，不伪装群聊他人发言 |
| A11 | 部分 | 前端不可解析引用显示不可用 | 当前三参数最终校验入口拒绝未知、跨主体、未实际读取 ID；旧 QA gate 更新并重跑 |
| A12 | 部分 | 删除确认和失效引用提示通过 | 对真实删除 command 核验本人/对象当前及历史画像、书摘快照清理；持久化重读仍无失效依据 |
| A13 | 部分 | 归属、事件过滤/展示上限、转义、阶段字段通过 | 服务发出的每条 phase 确有对应执行，不包含内部推理/凭据/原始模型内容，服务事件有界 |
| A14 | 部分，文案已修复 | 启动响应待返但已轮询到 run 的前端取消、迟到轮询隔离通过 | 三个时间点的模型/工具取消传播与零提交；保存先完成时仍为 done；PRD-RV-02 已关闭，取消链路仍待验收 |
| A15 | 部分 | runId 请求、重复点击、旧按钮、新任务隔离与 cancelling 互斥通过 | 真实服务重复/定向/省略 runId 的兼容行为与旧回调零提交 |
| A16 | 部分 | 挂载仅读取、不重新启动；卸载不取消、不复活 DOM 通过 | 真实浏览器关闭重开，仍读到数据库已保存画像/有效依据；运行任务没有被页面生命周期取消 |
| A17 | 部分，提示缺陷已修复 | interrupted 展示、保留旧画像渲染通过；关闭提示缺陷已复现 | 隔离数据库中 running/cancelling 重启后为 interrupted、旧画像保留；PRD-RV-01 已关闭，不代替运行验收 |
| A18 | 未独立执行 | 后端文档说明进度独立文档 | 实测进度不递增业务 revision；真实输入改变导致旧版本不保存，不能用绕过校验处理 |
| A19 | 部分：源码与测试报告已核对，未本人重跑 Java | `838c21f` 专用预算错误映射已核对；TL 的 ProfileBudgetTest 报告 3/3 通过，含 6 次模型调用上限和静默超时；前端无假百分比 | 工具输出上限及实际任务不保存的其余证据；真实运行中预算错误通过 HTTP/页面呈现。不能由 runtime 测试替代整条链路 |
| A20 | 部分，提示缺陷已修复 | 错误展示与文本转义通过；关闭提示缺陷已复现 | 模型/工具/校验失败时零覆盖，错误脱敏并区别无书；PRD-RV-01 已关闭，不代替运行验收 |
| A21 | 前端部分通过 | 人物/本人切换、晚到轮询、任务及书摘归属通过 | 完整服务运行时浏览器切换的 smoke，不把 Node 边界替身视为真实服务证据 |
| A22 | 部分，提示缺陷已修复 | 旧画像、旧 job 无 runId、缺省知识状态及序列化重读通过 | 真实旧 HTTP 客户端接口兼容；原错误关闭能力 PRD-RV-01 已复验恢复 |
| A23 | 部分 | 特殊字符安全渲染、旧策略来源展示及修正文案通过 | 实际旧策略生成/保存、参考回应复制及人工发送边界；不要求改造策略输入 |

## 交给 TL 的最小收口顺序

1. 两个已确认问题和删除确认说明已完成前端复验；目标提示遗漏仍由 TL 安排现有前端补齐。关闭提示已覆盖轮询与新任务隔离，无需新增角色或接口。
2. 在固定集成 SHA 重跑独立 Java gates 及真实底座的确定性链路；QA 报告同步更新 A04/A11 等旧基线失败，逐条给出通过或未测依据。
3. 用已经授权的隔离环境留出 A09/A12/A14/A16/A17/A23 真实运行记录；真实模型与可控模型分列，数据库验证和浏览器操作分列，不用总测试数抵消缺证。
4. 若现有底座/隔离环境无法兑现承诺，按原范围规则报告 TL 再由用户决定，不临时增加新服务、改底座或暗降门槛。


## 复验补充：461b3dc → 集成 7cf1ab5

复验目标：前端 `461b3dcb346e8a82b146e34e89b1213f2bf3b246`，已集成为 `7cf1ab5b8c2d8038f5f74040974e63f788c5feb1`。`git diff 461b3dc 7cf1ab5 -- web/src` 无差异，测试前后集成 SHA 相同。重新执行上文完整 Node 命令，结果 **56 tests / 56 pass / 0 fail / 0 skipped**，退出 0。

| 项目 | 最新状态 | 本轮独立复验依据 |
| --- | --- | --- |
| PRD-RV-01 关闭提示 | 已关闭（前端边界） | error/interrupted 各运行真实挂载与点击处理，关闭只改本地 UI、不发送 POST；轮询与页面切换不复现旧提示；新 run 提示仍显示，旧关闭按钮不能隐藏新 run |
| PRD-RV-02 取消文案 | 已关闭（前端渲染） | 无旧画像不声称存在旧画像，有目标旧画像才追加说明；本人无画像而他人有画像时也不误报；新文案明确本次结果未保存 |
| 删除确认说明 | 已关闭（文案及交互） | 真实打开确认弹窗，文案明确“引用它的相关当前及历史画像会被移除，需重新生成”，并保留策略删除语义；打开确认时尚未发送删除请求 |

这些修复未新增服务、接口、持久化能力或业务功能。刷新整页后重新读取服务终态符合既有语义；本地隐藏提示不声称更改服务任务结果。

**仍未通过的整体门槛：** A01–A23 表中所有后端/真实运行缺证维持原状态。在首轮复验时，TL 通报静默流取消时 provider 订阅未释放，后续 `838c21f` 已可见流取消适配；本报告尚未收集该项最新执行验收证据，A14 取消传播与零提交不能记为通过，更不能由本次前端 56/56 推导通过。真实 smoke、真实模型、数据库恢复及真实浏览器重开仍待执行。目标表单文案遗漏已由 TL 安排前端处理；预算错误文案已按下节核对关闭，不再列为待修缺陷。


## 预算文案更新：838c21f

按 TL 通知核对固定提交 `838c21f`（预算修复后端 `a59b21e`，集成 `213770e`）：

- `ProfileRuntime.java:18/89/106` 定义专用 `BudgetExceeded`，第 7 步开始前置标记并阻止执行，循环收尾后仍抛出该专用异常。
- `RanchAnalyzer.java:65` 将其映射为「已达到6步预算，本次结果未保存。」，不再落入通用模型/引用/版本失败文案。原预算辨识度意见关闭。
- 独立读取 TL 工作树 `target/surefire-reports/TEST-dev.garden.ProfileBudgetTest.xml`，记录为 3 tests / 0 failures / 0 errors / 0 skipped；用例为静默模型超时、阻止第 7 次调用、空库与未命中的有限结果。已核对测试源码，预算用例断言实际模型调用次数为 6 且返回异常含预算信息。**这是读取 TL 的实跑报告，不是产品角色重跑 Java，也不证明真实模型或 HTTP/数据库端到端通过。** 报告 XML 未独立绑定 Git SHA；TL 已明确本轮通过，源码检查对象固定为 `838c21f`。
- 目标表单原范围文案由 TL 安排现有前端补齐，本次未收到或复验其完成提交。
- 真实验收仍未发生；A19 完整运行证据、A14 取消传播等门槛保持未完成，不将此文案修复扩展为全部验收通过。
