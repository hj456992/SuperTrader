# 爱聊画像 Agent：TL 集成交付验证

执行监督任务：01a0d244-0bdd-71e2-b824-6ac31bb31078。沿用原有五个角色，日常协调不再发送架构任务。

## 范围与位置

工作树为 work/ailiao-upgrade/tl，分支 ailiao/tl-agent-upgrade，接管起点790a6b1。本人/对象画像接入真实DSH/AgentScope书籍和聊天工具循环，保留词项检索、轮询、现有PostgreSQL文档和界面。目标不进入人物画像输入；增加必要的阶段、引用校验、取消保存屏障和重启中断记录。

未新增服务、数据库、付费依赖、向量库、多Agent、自动发消息、平台全历史订阅或公共底座改造。原main和48740实例未用于测试，未推送远端。保留用户指定工作树作为本地可运行交付。

## 确定性验证

| TL执行项目 | 结果 |
| --- | --- |
| 55167aa最终完整release Java，含全部QA/gates | 86项，0失败/错误/跳过 |
| Node组件与独立验收 | 60项，0失败/跳过 |
| Python启动器及smoke探针 | 15项通过 |
| 飞书插件Java | 31项通过 |
| 最终生产代码build.sh | 退出0，业务/飞书JAR和UI构建完成 |
| 非业务真实宿主装配 | 9插件全部active |
| QA补充时间回归 | 同5项修复前红、修复后绿，已纳入最终86项 |

模型替身测试使用真实DSH插件、AgentScope和工具调度；独立QA保存断言使用Repository seam，不冒充真实PG。覆盖两主体观察改变保存、未读/跨主体引用拒绝、三处取消迟到零保存、保存先占屏障保持done、静默流取消/超时释放源订阅、6步预算、进度修订隔离、恢复、删除、无书/无命中和旧数据兼容。

构建使用交接的任务私有Maven repo，不修改共享缓存或底座。实际DeepSeek JAR的纯wire序列化测试确认每步max_tokens为整数6000。

## 真实隔离验证

只用原创虚构聊天与练习册，端口48749，既有postgres维护库内独占schema ailiao_profile_smoke_0e2d7c0a95d2。凭据与Cookie仅内存，没有平台采集和真实聊天。

完整业务宿主10插件全部active。5bfd899的真实DeepSeek/HTTP/PG验证：
- 无书本人2步，empty_library、明确限制、无书摘。
- 有书对象3步、本人2步，检索后继续模型，各有可核对书摘。
- 既有策略保存，改目标前后人物画像完全相同。
- 错runId无效、正确和重复取消完成，旧画像/revision无迟到覆盖。
- 新Cookie会话可读旧画像；真实浏览器关闭标签再开也可读。
- 仅异常中断本任务进程后重启，同run为interrupted，旧画像与revision14保持。
- 删书前current/history各有2条真实引用，删除后均撤回。
- 恢复有书状态到revision18；探针保存窗口误判经源码和回归确认，已保存对象未重发，仅补剩余本人。

时间修复后的最终生产候选d62253a121b0ae90476212b5245a53fa2c873089，重新装配10插件，单次本人真实定向复验：2步、4条认识、1书摘、revision18→19、done。TL独立读取确认明确“无发言时间，无法判断频次与持续性”，不再断言同一天。其他真实场景未因同一缺陷重复调用模型，验证版本如实分列。

最终生产JAR SHA-256：a9a873e917958fdbc8f482f0c0dba5962ca0458b1960186f741ba459f4438711。

## 审查中修复的问题

1. 终态提示错用取消接口：按runId本地关闭，首次取消文案按实际旧画像判断。
2. 删书确认：明确相关当前及历史画像会移除。
3. 静默流取消只改状态不释放订阅：应用作用域正确取消源订阅，取消/超时原断言红绿验证。
4. 真实供应商拒绝6000.0：现有请求边界规范Integer6000，保留预算，实际serializer回归。失败零保存，没有循环重试。
5. 探针误拒业务保存与job独立终态写入间窗口：只允许同run已存画像、revision恰+1的saving过渡，仍等待done；其他变化失败，不改生产。
6. 录入/采集时间误当发言日期：原at仍存储/排序，模型副本移除操作时间，仅提供来源确证spokenAt；初始和工具一致，旧日期未知，不迁移数据。

## 证据和限制

- [产品A01–A23](../product/profile-agent-acceptance.md)、[真实质量审阅](../product/profile-agent-real-quality-review.md)
- [独立运行链路](runtime-profile-agent-review.md)、[实际传输](provider-transport-review.md)、[时间来源](time-provenance-review.md)
- [真实运行记录](../ops/profile-real-smoke-report.md)、[运行/回滚手册](../ops/profile-agent-runbook.md)
- [真实浏览器报告与截图路径](../frontend/browser-acceptance-5bfd899.md)

TL已查看原始对象引用截图，聊天与书籍方法分区正确。浏览器此次未制造error/interrupted、运行中取消、多个对象/不同书籍，这些有确定性覆盖但不声称均已浏览器手测。截图和AX仅在忽略的本地target，报告已提交。

真实模型样本有界，不能保证所有自然语言结论永远正确。旧对象摘要“用户描述”措辞歧义为P3，引用归属正确，本次未重生成对象，不冒称已复验。客户端取消不证明供应商端停止计算。未执行旧版生产回滚、真实数据迁移或原实例发布。

## 收尾

最终全量release在55167aa退出0：Java86、Node60，零失败/错误/跳过。最终生产文件与真实复验源d62253a无差异；之后只有测试及文档集成，不需替换已验证生产JAR。QA只读收尾审查未发现新阻断。

产品51a6e6e独立读取revision19，关闭本人RQ01日期与RQ03事实分类问题，确认无新增实质事实缺陷；旧对象RQ02保持P3未再次生成的说明。

运维c20eaa9记录正常停止持有的PID66300（退出143），准确删除任务独占schema。TL再次独立只读查询pg_namespace得到该准确名称count=0，普通bind48749成功，原48740仍由原PID99465监听。未删除其他schema/库、工作树、构建产物或截图证据。

本地验收交付完成；main仍为b410f0f且工作树干净，没有发布替换原实例或推送远端。集成提交的最终状态由交付消息给出。
