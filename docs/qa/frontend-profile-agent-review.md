# 前端独立验收复核

日期：2026-09-24。受 TL 委托审阅前端原始提交 `ed83e4f`，在 QA 分支 cherry-pick 为 `00febeb`。未修改任何生产前端文件。R01 书架/上传文案修正已由 TL 安排，不重复报缺陷。

## 结论

本次范围内未发现新的阻断缺陷。`node --test web/src/*.test.js tests/acceptance/*.test.mjs` 实跑 **49 tests / 49 pass / 0 fail / 0 skip**，其中本阶段新增独立用例 7 个，文件为 `tests/acceptance/frontend-profile-agent.test.mjs`。测试只替换 DOM/计时器/HTTP 边界，挂载真实 `apply`、请求序号和点击处理程序；没有复制生产状态机。

| 独立情景 | 产品对应 | 实际断言 |
|---|---|---|
| 本人当前与历史画像跨两个人物解析 me | A01/A10/A22 | 三条本人原文均显示；两个人物的 them 与背景原文均不显示；失效引用明示 |
| 对象当前/历史画像及反证的来源隔离 | A02/A03/A06/A11 | 对象正反原文可见；另一人物、本人、背景不可解析为对象依据；书摘不进入人物事实区域 |
| 旧画像、无书、无命中的区别 | A07/A08/A22 | 旧画像不虚构新知识状态；empty_library 与 no_match 文案相互区分 |
| 本人任务与两个对象页面的切换 | A13/A21/A23 | 进度仅在归属页面出现；其他页面保持全局任务互斥；消息、runId 转义，内部事件不展示 |
| 取消后切换乙，再返回旧轮询 | A14/A15/A21 | 旧甲快照不覆盖取消状态、不污染乙画像；取消提交正确 runId；无错误完成通知 |
| 取消请求未返回时重复点击，随后观察新任务 | A14/A15 | 仅发一次取消；旧按钮不能取消新 run；显示新 runId |
| 页面卸载时状态读取尚未返回 | A16/A22 | 不发送取消请求、不重新生成、不恢复已清空 DOM、不弹完成提示 |

## 代码审阅核查点

- `web/src/ranch-view.js`：selfPage 显式赋予 `id=self`，`selfEvidence` 汇总跨人物 me；`profileEvidence` 据主体限制 speaker，并排除书籍 ID、错误自述/备注 ID；历史画像复用相同引用边界。
- `web/src/ranch-job.js`：running/cancelling 均视为活跃；任务 targetId 不同则不在当前人物/本人页面展示。
- `web/src/ranch-ui.js`：refresh 请求序号防止旧响应覆盖；完成通知要求相同 runId/targetId 且当前页面归属匹配；cancelPending 控制重复停止；runId 校验阻止旧按钮误伤；刷新和关闭不主动发起任务或取消。
- 当前及历史书摘使用画像自身快照。删除后的书摘是否已撤销依赖后端，前端渲染测试不能替代 A12 的真实删除命令验收。

## 明确限制

这是确定性渲染与事件/HTTP 边界测试，不是真浏览器 DOM 操作、真实服务或真实数据库验证；不证明后端取消传播/保存屏障。A09 工具观察后模型继续、A12 删除引用、A14 三处零提交、A17 重启、A18 revision、A19/A20 预算与失败仍等待后端提交。A21 前端切换与 A22 旧画像渲染部分已通过，完整 HTTP 兼容仍需集成验证。真实模型未执行。

TL 已通知后续 Maven 可使用后端任务专用仓库，但本次只跑 Node；未读取/写入专用仓库或安装依赖。
