# 前端真实浏览器验收清单

本清单准备于候选 b5df7ee 阶段；后续执行 TL 授权候选 5bfd899，实际操作结果见 [验收记录](browser-acceptance-5bfd899.md)。开始前必须等待执行 TL 启动通知；验收目标仅为运维隔离环境 `http://127.0.0.1:48749/`，不操作原 48740。若页面不是虚构验收状态，立即停止并报告，不自行导入或生成。

## 有界操作与证据

1. 打开指定隔离首页，记录页面/目标 URL 与实际人物名称，确认是运维恢复的有书成功状态。
2. 本人画像：查看保存结果；分别展开聊天/自述依据和书籍方法，核对标题分区、原文、未知/范围；截本人图。
3. 对象画像：在目标人物页展开聊天、书籍（如有反证亦展开），截图；来回切换本人/对象及另一虚构人物（若有），确认内容归属随页面变化，记录实际所见。
4. 重开同一隔离 URL，再进入同一人物，核对已保存 summary/引用仍可见。此动作只读取，不点击生成或更新。
5. 打开现有人物编辑表单，记录“交往目标用于相处建议，不改变人物事实。”，截图后关闭，不保存。
6. 在资料书架点击删除入口，记录确认文案明确相关当前及历史画像移除及策略影响，截图后取消，不确认删除。
7. 仅当实际存在 error/interrupted 提示时点击“关闭提示”，记录关闭前后及一次正常轮询后的页面；若无此状态标记未测，不人为制造失败、不调用额外模型。

不操作平台导入、生成/更新画像或攻略，不访问真实材料或运行配置，不修改/删除数据。不得把此前 Node/HTTP/Java 探针结果作为本次浏览器通过证据。

## 留证

每项记录实际 URL、页面可见标识、操作和观察结果、通过/失败/未测。截图保存到本前端工作树忽略的 `target/browser-acceptance-5bfd899/`，向执行 TL 返回绝对路径；不提交图片或私有状态。实际验收以 TL 最终通知的候选和环境为准，若候选变化须改标识，不把旧截图冒充新构建。
