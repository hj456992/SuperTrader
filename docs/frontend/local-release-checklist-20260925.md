# 4754cb1 本地发布：前端只读准备与最小影响建议

## 本批状态与边界

固定生产候选为 `4754cb1`；TL 后续 `b679668` 仅计划文档，不作为新的生产制品版本。本文件来自对候选 Git 对象、已提供架构文档和本机只读底座加载器的核对。尚未访问原 `48740`，没有启动浏览器、模型、服务、构建或平台导入，也未查看用户人物/聊天/运行配置。

本批目标是已验收版本的本地使用入口发布，不新增产品功能。实际切换、数据备份与回滚由执行 TL/运维负责；前端等待明确的“发布完成，可以验收”通知。旧 `48740` 进程/旧目录状态仅来自 TL 交接，不冒称本轮已重新验证。

讨论依据为 `outputs/ailiao-architecture-20260925/爱聊整体架构与核心功能.md`；它不是全量扩容规格。流式、推荐问题、记忆等仍待决定。

## 只读代码核对

| 落点 | 已确认事实 | 对发布核验的含义 |
| --- | --- | --- |
| `web/public/index.html` | `/ranch.css`、`/vendor/bootstrap.js`、`/vendor/client-modules.js`、模块 `/host.js` 均用根路径；标题为“爱聊 · 关系牧场” | 检查完整资源组，不能只看首页返回 200 |
| `web/src/host.js` | 精确 `/conversations.html` 选择 `garden-ui`，其他入口选 `ranch-ui`；boot rev 固定 `ranch-1`、模块 rev 固定 `1` | rev 字符串不是本次 Git 版本标识；须比较批准构建制品 SHA-256 |
| `HttpApi` | 路由 `/` 映射 `/index.html`；所有响应 `Cache-Control: no-store`；JS/CSS 分别 `text/javascript` / `text/css` | 用 GET 核验状态、MIME、缓存头和内容指纹；不能用“无文件名 hash”直接断言缓存错误 |
| `HttpApi` | Host 只接受 `127.0.0.1:<port>`，非 API 仅支持 GET | `localhost:48740` 和 HEAD 不能作为等价健康检查；本批固定准确 URL |
| `HttpApi` | 每次进程启动产生会话 Cookie；首页/旧会话页会更新 Cookie | 发布重启后旧页面可能持旧会话，用户需保留草稿后重新打开，不能把 401 全当接口坏了 |
| `ranch-ui.js` | mount 立即 GET `/api/ranch-state`，随后 1.3s/5s 轮询；主页会渲染人物名和状态 | 打开主应用不是“只读空壳”：会自动读取业务内容，不适合作为本批零内容接触烟测 |
| `ranch-ui.js` | 初次失败显示重新加载入口；后续轮询失败提示重试；未提交攻略草稿存在页面内 Map | 不擅自刷新/关闭用户已有标签；重开会丢失未提交输入，no-store 不解决草稿和旧页面内模块问题 |
| 底座 `frontend/vendor/plugins/dsh-client-modules/client.js` | Loader 在当前页面用 Map 保存 factory/loadCache | 服务发布不会自动替换已打开页面里的模块；这不是磁盘持久缓存的证据 |
| `build.sh` / `web/build.mjs` | 构建脚本先建 `web/dist/vendor`；打包器复制静态文件和底座 loader，构建 host 与两个页面插件 | 只运行 `npm ... build` 的干净目录风险仍需由运维完整构建流程处理；本批不改构建器 |
| `run.py` | `GARDEN_WEB` 指向当前启动目录的 `web/dist` | 远端 main 对齐不证明运行实例或静态目录已切换，须以发布清单和响应指纹复核 |

可识别的已交付能力信号存在于 `ranch-ui.js` 构建体：书架说明“生成画像或攻略时”、目标提示“交往目标用于相处建议，不改变人物事实。”、终态 `dismiss-job`、聊天/书籍/反证分区。它们只辅助识别前端版本；不能证明本机 Agent、模型、取消或数据库链路已通过。

## 发布后零业务内容烟测方案（待 TL 通知，尚未执行）

### 前置交接

TL/运维提供实际发布目录、绑定端口、候选标识和本次构建制品清单/指纹。必须使用本次批准的构建输出作为参考，不能拿前端角色较早的 `web/dist` 代替。只登记候选、资源路径、状态、MIME、缓存头、字节数和 hash；不采集环境变量、Cookie 值或业务响应。

### 静态资源检查

1. 通过只读 HTTP GET 获取 `/index.html` 文本，不在浏览器执行它；只解析固定静态引用、标题并核对批准版本。必要时同样读取 `/` 文本，核对与 `/index.html` 相同；不会执行其中脚本。
2. 对以下固定资源逐个读取并核对 `200`、合理非空长度、正确 MIME、`no-store` 和与批准构建匹配的 SHA-256：
   - `/ranch.css`
   - `/host.js`
   - `/ranch-ui.js`
   - `/vendor/bootstrap.js`
   - `/vendor/client-modules.js`
3. 兼容入口仅静态读取 `/conversations.html`，核对仍引用 `/style.css`、`/assistant.css`、相同两个 loader 与 `/host.js`；核对 `/garden-ui.js` 和这两个 CSS 的指纹。不执行旧会话页脚本。
4. 浏览器只直接打开已确认的静态文本资源，例如 `/ranch.css`、`/ranch-ui.js`。顶层资源文本展示不会挂载应用或读取人物状态；记录实际 URL 与资源可读性。禁止把这一结果称为“完整 UI 交互通过”。
5. 如有 404、HTML 代替 JS、MIME 不符、构建指纹混批或 Host/路径不符，保留不含内容的元数据，立即报 TL/运维；不自行重建、清缓存、切版本或重启。

全程不请求 `/api/*`，不打开 `/`、`/index.html`、`/conversations.html` 的可执行浏览器页面，不进入人物、本人画像、书架数据页，不调用任何 POST。下载的静态代码仅在本地核验，记录不含用户信息。

### 能证明与不能证明

通过后可写“4754cb1 对应批准制品的静态入口/资源已可达，浏览器能打开静态资源，指纹及缓存头匹配”。不能写“真实用户页面已完整验收”“当前模型可用”或“旧画像兼容已在用户数据上验证”。完整动态 UI 的既有证据来自先前隔离虚构数据验收，不能搬成本次发布实测。

若 TL 后续要求主应用实际渲染，需要另行明确允许的零内容或隔离方式：主入口会自动读取业务状态，即使只截侧栏也已经发起该读取。本批不擅自用拦截业务 API、修改页面状态或新建代理服务来绕过边界，不申请读取用户真实聊天以补足测试。

## 交互回退风险与最小处理

| 风险 | 可见后果 / 限制 | 本批处理 |
| --- | --- | --- |
| 用户旧标签未重载 | 页内旧 JS 继续运行，新后端可能已上线 | TL 提醒用户先保存/复制草稿再自行重开；不代操作其现有标签 |
| 重启后旧会话 Cookie | 已开页轮询报连接/会话失败 | 用准确主页地址重新打开取得新会话，不自动反复提交操作 |
| 资源组跨版本混用 | 插件启动失败、样式缺失、文案与行为不一致 | 比较全部批准制品指纹；失败交运维统一恢复，不只替换一个 JS |
| 初次加载接口失败 | “暂时无法打开牧场”与重新加载入口 | 当前零业务 API 检查不覆盖这一动态回退；沿用确定性证据并标未实测 |
| 旧终态提示和未提交输入 | 本地隐藏记录与草稿不跨页面重开持久化 | 不把重开视为自动恢复工作；不扩展存储或记忆功能 |
| 用户材料触发不同业务组合 | 实际内容或模型质量问题未必在静态检查暴露 | 不使用用户聊天做夹具，另由产品收敛匿名/虚构复现场景后安排 |

以上是发布注意事项，不据此认定已存在发布阻断；当前没有在线检查结果。若有运行时阻断，前端只记录证据并同时通知 TL/运维，由其决定恢复步骤。

## 给产品的后续最小 UI 影响与验收建议

以下均为讨论输入，不是新增实施承诺。先完成本批发布；后续每批单独由 TL 确定范围。

| 架构事项 | 最小 UI 影响建议 | 最小验收建议 / 进入条件 |
| --- | --- | --- |
| 来源更正、撤销与内容版本 | 若后续先确定后端失效规则，优先复用已有“待更新/来源不可用”及引用详情，不新建消息管理页 | 用一个虚构来源的更正/撤销检查当前与历史引用；先明确更正后保留、删除和重生成语义，避免 UI 自行猜测 |
| 已确认发言日期与录入时间 | 如产品确认需要展示，范围限于既有引用详情中的已确认发言日期/日期未知，不改变画像逻辑 | `spokenAt` 确证值、无值、录入时间不冒充发言时间三种夹具；先确认字段和文案，暂不实施 |
| 书籍章节/段落优先切分 | 保留现有书名、位置、原文详情；若返回契约不变可能无需 UI 改动 | 命中片段与扩展上下文来源可核查，章节/页码不伪造；不同时引入向量库或新检索页面 |
| 缩小 revision 冲突域、结果与 job 一致性 | 先保持现有保存冲突提示与草稿保留，不增加并发任务面板 | 不同人物修改与同人物冲突、保存已成而终态滞后的夹具；后端契约/恢复规则先确定 |
| 入库可靠性与进度复用 | 只有后端出现真实新状态时再决定是否复用现有上传弹窗/进度；不预先做队列、恢复入口或假百分比 | 失败后是否保存、能否重试、重复提交去重三种明确定义；属于后续可选，不以此阻塞本批发布 |
| 画像结论质量 | 产品先收集少量虚构的“原话—结论”反例，UI 维持直接信息/推测、限制与反证 | 用既有引用区核对主体、动作方向、时间与转述标签；不加入置信评分或自动评判新功能 |
| token 流式、推荐问题、跨会话记忆 | 保持待决定；不添加入口、占位按钮、伪流式或自动调用 | 必须单独确认用户问题、成本、取消与保存语义、范围；当前未知项不等于推荐问题，持久画像不等于会话记忆 |

建议产品优先把来源生命周期和日期/证据展示规则写成小范围验收样例，供 TL 选择下一批；不默认全部实施。需要新增服务/库/付费依赖/公共底座大改、多 Agent 或全历史订阅时，停止该扩展并由 TL 报用户决定。

## 本次交付验证

仅新增本清单文档。已核对 `git show 4754cb1:<path>` 指定代码与架构文档，不执行在线 smoke，不运行或修改原实例。文档提交前检查 Git 差异范围及 `git diff --check`；后续实际发布检查须另记时间、批准制品指纹、结果及未测边界。
