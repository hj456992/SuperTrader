{
  "name": "supertrader-demo-e2e",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Playwright E2E for the Agent Runtime Harness Demo. Runs against a real locally-started backend (:8080) + frontend (:5174). NEVER connects SimNow/CTP.",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
